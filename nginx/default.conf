upstream webfib {
    server webfib:3000;
}

upstream webfib_server {
    server webfib_server:5000;
}

server {
    listen 80;

    location / {
        proxy_pass http://webfib;
    }

    location /api {
        rewrite /api/(.*) /$1 break;
        proxy_pass http://webfib_server;
    }
}
